<h2>Create a new task:</h2>

<%= simple_form_for todo, remote: true do |f| %>
  <%= f.input :title,
              autofocus: true,
              required: true %>

  <%= f.input :description %>

  <%= f.input :due_date %>

  <%= f.submit "Create", class: 'btn btn-success' %>
<% end %>